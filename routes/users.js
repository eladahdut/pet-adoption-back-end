var express = require("express");
var router = express.Router();

/* GET users listing. */
// router.get("/", function (req, res) {
//   res.send("respond with a resource");
// });

/* POST signing up a new user. */
router.post("/signup", function (req, res) {});

module.exports = router;
